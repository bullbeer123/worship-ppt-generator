# 主日礼拜PPT生成器 V2 — MRD

> 版本: 2.0 | 日期: 2026.06.07 | 作者: 牛智楷

---

## 一、项目背景

### 1.1 产品现状

V1 已上线运行（https://bullbeer123.github.io/worship-ppt-generator/），核心流程可用但存在以下问题：

| 维度 | V1 现状 | 痛点 |
|------|---------|------|
| 解析准确率 | ~70% | 诗歌跨诗集序号冲突、经文链接引用、歌名标点变体未覆盖 |
| 音频体验 | 不稳定 | CORS 代理依赖、缓存命中率低、部分歌曲无音频 |
| 输入方式 | 纯文本粘贴 + 拍照 | 拍照识别不稳定、无进度反馈 |
| 交互反馈 | 弱 | 操作结果不明确、无错误定位、无操作引导 |
| 视觉设计 | 基础 | 移动端适配差、布局拥挤、无设计层次 |
| 错误处理 | 静默失败 | 用户不知道哪里出问题 |

### 1.2 用户画像

| 角色 | 使用频率 | 核心场景 | 痛点 |
|------|:--:|------|------|
| 主日礼拜负责人 | 每周1次 | 粘贴通告文字 → 生成PPT → 投屏播放 | 解析错误导致歌词/经文不对；音频加载慢 |
| 教会同工 | 每月2-3次 | 同上，偶尔拍照识别节目单 | 拍照识别不稳定；不知道如何修正错误 |
| 管理员(智楷) | 按需 | 远程排查问题、功能迭代 | 无法远程了解用户实际问题 |

### 1.3 用户故事

1. 粘贴通告 → 点击按钮 → 1 秒内看到结果 → 所有字段正确填入 → 生成幻灯片 → 投屏
2. 错了解析结果 → 明确看到哪个字段错了 → 手动修改 → 重新获取歌词/经文
3. 拍节目单照片 → 自动识别 → 核对修正 → 生成幻灯片
4. 手机打开 → 粘贴文字 → 一路完成所有操作

---

## 二、市场需求

### 2.1 核心诉求

**「零学习成本、零容错率、零等待」**

- 用户不关心技术细节，只关心结果正确
- 每周使用一次，不能每次都有 bug
- 手机端操作，需要大按钮、大字体、简单流程

### 2.2 竞品参考

| 竞品 | 优势 | 不足 |
|------|------|------|
| pptxworship.com | 专业 PPT 模板丰富 | 需手动编辑 PPT，不是粘贴→生成 |
| 国内同类工具 | — | 市场上无同类中文礼拜 PPT 自动生成工具 |

**结论：品类空白，先发优势，核心壁垒是解析精度。**

---

## 三、产品目标

### 3.1 北极星指标

**一键生成成功率 ≥ 95%**（从粘贴到正确 PPT 下载，全程无需人工修正）

### 3.2 V2 目标

| 指标 | V1 当前 | V2 目标 |
|------|:--:|:--:|
| 解析正确率 | ~70% | ≥ 95% |
| 音频命中率 | ~60% | ≥ 90% |
| 拍照识别可用率 | ~60% | ≥ 85% |
| 移动端可操作性 | 差 | 好 |
| 操作完成时间 | 2-3 分钟 | ≤ 60 秒 |

---

## 四、功能需求

### 4.1 Phase 1：核心稳定（2 周）

#### F1.1 智能解析引擎重构

**现状问题清单**：

| 编号 | 问题 | 根因 | 状态 |
|:--:|------|------|:--:|
| P1 | 证道经文链接引用不识别 | `fetchBible` 旧正则 + `题目` split | ✅ 已修 |
| P2 | 诗歌跨诗集序号冲突 | xuhao=133 在 shiji=0/1 同时存在 | ✅ 已修 |
| P3 | 附录诗歌前缀「附」不识别 | 数字剥离忽略「附」 | ✅ 已修 |
| P4 | 诗篇编号在 bibleBooks 中缺失 | `诗` 未列入书卷列表 | ✅ 已修 |
| P5 | 歌名中数字误判为歌曲边界 | 拆分正则过激 | ✅ 已修 |
| P6 | 微信连续文本换行丢失 | 预处理逻辑不完善 | ⚠️ 待优化 |
| P7 | 赞美/新编赞美之间空格作为分隔符 | 未充分利用空格语义 | ⚠️ 待优化 |
| P8 | 经文引用中的中文标点（全角冒号、间隔号） | Unicode 规范性 | ⚠️ 待优化 |

**需求**：
- 统一的解析管道（预处理→字段提取→校验→修正）
- 每个字段解析独立、可测试、可配置
- 解析结果置信度标注（高/中/低），低置信度字段高亮提醒

#### F1.2 音频预加载优化

**现状**：CORS 代理不稳定 + IndexedDB 缓存命中率低

**需求**：
- 本地高频歌曲库扩充（当前仅 2 首 → 目标 50 首）
- 音频列表预热：页面加载时预取 sgbList，提前建立歌曲→URL 映射
- 播放优先级：本地文件 > IndexedDB 缓存 > 直接 URL > CORS 代理
- 后台预加载队列：一键匹配后立即并发预下载所有歌曲音频
- 播放状态提示：「已缓存⚡」「下载中⏳」「无音频⚠️」

#### F1.3 拍照识别优化

**现状**：Qwen VL 识别不稳定，无错误反馈

**需求**：
- 识别结果展示对比面板（原图 + 识别字段 + 置信度）
- 逐字段可手动修正
- 低质量照片检测（模糊、光照不足）并提示重新拍摄
- 支持连续拍照（多页节目单）

#### F1.4 错误反馈体系

**需求**：
- Toast 通知分级：成功(绿)、警告(黄)、错误(红)
- 解析失败的字段红色边框标记
- 操作进度条（一键匹配 3 阶段：解析→搜索→生成）
- 错误日志面板（开发者模式，可查看 API 调用详情）

### 4.2 Phase 2：体验提升（2 周）

#### F2.1 移动端自适应

**需求**：
- 响应式布局：桌面 3 栏 / 平板 2 栏 / 手机单栏
- 按钮最小点击区域 48x48px
- 粘贴区在移动端自动展开、键盘弹起时滚动到可见区域
- 幻灯片预览区在手机端支持左右滑动手势
- 字号对比度符合 WCAG AA

#### F2.2 操作流程简化

**需求**：
- 首页引导式流程：3 步向导（粘贴→确认→生成）
- 常用操作快捷入口（粘贴后自动解析，无需点击按钮）
- 历史记录快捷恢复（最近 5 次，带时间戳和预览）
- 一键重置（清空所有字段 + 清除缓存）

#### F2.3 幻灯片预览增强

**需求**：
- 目录缩略图导航（12 张预览图并排）
- 全屏模式自动隐藏工具栏
- 幻灯片间切换动画（淡入淡出）
- 直接编辑：双击幻灯片文字框→就地编辑→实时生效

### 4.3 Phase 3：智能化（按需）

#### F3.1 智能纠错

**需求**：
- 常见拼写错误自动修正（「耶酥」→「耶稣」、「诗偏」→「诗篇」）
- 日期格式自动识别（2026.6.7 / 2026-06-07 / 6月7日 → 统一格式）
- 赞美序号补全（输入「131」→ 自动补全「131 望楼的祷告」）
- 诗篇编号校验（1-150 范围外提示）

#### F3.2 模板系统

**需求**：
- 内置 3 套配色模板（深紫/深蓝/墨绿）
- 字体系列可选（微软雅黑/思源黑体/苹方）
- LOGO 水印位置可调
- 自定义页面的布局模板（标题+文字/左图右文/上图下文/全文全图）

#### F3.3 多语言支持

**需求**：
- 中英双语歌词自动检测并分行显示
- 英文礼拜格式兼容（Sunday Worship → 日期提取）
- 繁体中文识别和转换

---

## 五、非功能需求

### 5.1 性能

| 指标 | 目标 |
|------|------|
| 首屏加载 | < 2 秒 |
| 文本解析 | < 300ms |
| 歌曲搜索 | < 1 秒/首 |
| 幻灯片生成 | < 1 秒 |
| PPT 导出 | < 3 秒 |

### 5.2 可靠性

- 域名：bullbeer123.github.io/worship-ppt-generator/
- 部署：GitHub Pages + Actions
- API 依赖：发光如星 (top237.top) + 通义千问 (aliyuncs.com)
- API 不可用时的降级策略：缓存歌词 + 本地音频 + 手动输入
- 离线能力：Service Worker 缓存静态资源 + IndexedDB 缓存歌曲数据

### 5.3 兼容性

- 浏览器：Chrome/Safari/Edge 最新 2 个版本
- 移动端：iOS Safari / Android Chrome 微信内置浏览器
- 屏幕：320px ~ 2560px

### 5.4 安全性

- API Key 不暴露在 HTML 中（V1 内嵌 QWEN_KEY，V2 改用环境变量/后端代理）
- 用户数据仅存 localStorage，不上传服务器
- 图片上传不持久化（用完即删）
- 音频缓存限制 100MB，LRU 淘汰

---

## 六、技术约束

| 约束 | 说明 |
|------|------|
| 纯静态部署 | GitHub Pages，无后端服务器 |
| 单文件架构 | 1 个 index.html 包含所有代码（便于分发） |
| 外部 API | 发光如星 (top237.top) — 歌曲搜索/歌词/音频；通义千问 — 多模态识别 |
| 无构建工具 | 不使用 Webpack/Vite，代码直接内嵌 `<script>` |
| 授权 | 仅供内部使用，不公开发布 |
| 维护者 | 智楷（非专职开发，按需迭代） |

---

## 七、风险与假设

| 风险 | 概率 | 影响 | 缓解 |
|------|:--:|:--:|------|
| 发光如星 API 改版或下线 | 中 | 高 | 缓存机制 + 本地音频库 + 备选 API |
| 通义千问 API 费用过高 | 低 | 中 | 限制调用频率 + 文本解析优先 |
| 微信内置浏览器兼容性 | 中 | 中 | 提供 Safari/Chrome 直达链接 |
| 用户输入格式多样化 | 高 | 中 | 完善的解析引擎 + 手动修正入口 |
| 维护时间不足 | 中 | 低 | 单文件架构、注释完善、Skill 积累 |

---

## 八、成功标准

1. **一线成功**：用户的叔叔每周能独立完成礼拜 PPT 制作，不需要智楷远程协助
2. **二线成功**：3 个教会同工开始使用，每人每月至少 2 次
3. **三线成功**：解析正确率稳定在 95% 以上，连续 4 周无新 bug 上报

---

## 九、附录

### A. 术语表

| 术语 | 说明 |
|------|------|
| 献上赞美 | 两首赞美诗歌，选自「赞美耶稣诗集」(shiji_index=0) |
| 特颂 | 特殊赞美，可选，不限诗集 |
| 新编赞美 | 两首新编赞美诗，选自「新编赞美诗」(shiji_index=2) |
| 诵读经文 | 诗篇，输入编号自动获取内容 |
| 牧师证道 | 第一讲和第二讲，含经文引用+讲题 |
| 777宣告 | 7大望楼、7大旅程、7大里程碑宣言 |
| 发光如星 | 第三方教会诗歌数据库 API (top237.top) |

### B. 数据流图

```
用户输入（粘贴/拍照）
    │
    ▼
[文本预处理] → 换行恢复、标点规范化
    │
    ▼
[智能解析引擎] → 字段提取 + 置信度标注
    │
    ▼
[并行搜索] → 歌曲歌词/音频 + 经文内容
    │
    ▼
[幻灯片构建] → 数据结构化 → 预览 → PPT导出
```

### C. API 依赖清单

| API | 用途 | 关键端点 | 容灾 |
|------|------|------|------|
| 发光如星 | 歌曲搜索/歌词/音频 | sgbList, searchByIdsLazyField | 本地缓存 + 手动输入 |
| 发光如星 | 圣经经文 | no-auth/bible/search | 错误提示 + 手动粘贴 |
| 通义千问 | 拍照识别 | qwen-vl-max | 文本解析兜底 |
