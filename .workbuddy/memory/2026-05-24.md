## 2026-05-24

### worship-ppt-generator 优化

**修改文件**: C:\Users\15201\WorkBuddy\2026-05-24-00-12-59\worship-ppt-generator-index.html

**修改1 - 经文/诗篇字号增大**:
- autoVerseFontSize() 各档位字号提升30-50%:
  - <=25字: 52→68px, <=45字: 44→56px, <=70字: 38→48px, <=100字: 32→40px, >100字: 28→34px
- 解决中文经文在16:9幻灯片上默认显示偏小的问题

**修改2 - 音频加载加速**:
- 页面加载时预初始化IndexedDB + 预加载固定歌曲(welcome #24, final #134)
- 播放远程音频时: 优先查缓存 → 缓存未命中直接播放原始URL(Audio元素天然支持跨域，无需CORS代理) → 同时后台缓存
- 首次播放延迟从等待CORS代理下载数秒 → 几乎即时

**注意事项**:
- GitHub MCP 集成无该仓库写入权限(403)，修改后的文件已保存在工作区，需用户手动替换到 bullbeer123/worship-ppt-generator 仓库
- 仓库文件SHA: 4bbdf24cdef80415801387fa38d203830aa04734 (修改前)

**修改3 - 拍照识别功能 (新增)**:
- 粘贴区新增「📷 拍照识别」按钮（青色渐变）
- 调用通义千问 qwen-vl-max 多模态API识别节目单照片
- 自动提取: 日期、赞美诗、诗篇、新编赞美、证道经文、论坛、777宣告等
- API Key: sk-b0145400ebd24d71b2741f11e9a1645f (嵌入HTML)
- 支持拍照(capture="environment")和相册选图
- 识别结果自动填入所有表单字段，滚动到编辑区供确认

**修改4 - V1→V2 DOM迁移崩溃修复**:
- 推送到GitHub时发现V2代码仍引用旧DOM结构，在oneClickGenerate/parseFromSidePanel/restoreLastRecord多处崩溃
- 全面修复：移除 `.side-label`/`.side-hint`/`event.target.closest()` 依赖，改用 `showPhotoLoading()`
- fetchLyrics/fetchSermonSilent/fetchLyricsSilent/fetchBible：null guard
- manualSave、closeSlideshow：移除旧DOM引用
- prefetchAudioInBackground：移除死掉的CORS代理 `api.codetabs.com`
- buildSlides：hymn title slides 添加缺失的 `audioKey`
- parse777AndShowButtons/renderSlide：添加 null guard
- saveContent→manualSave 重命名修复
- shiji_index null 兼容修复（赞美耶稣诗集用null不是0）
- 新增V2缺失DOM元素：`#decl-quick-btns`、`#img-cover`、`#img-ending`

**修改5 - 功能改进**:
- 标签文案：论坛→论坛要点，宣告→祷告
- checkPraiseNames() 函数：检查赞美名称完整性，显示ok/warn/err状态badge
- 可编辑歌词 textarea 内联到每个字段卡片 + 本地音频选择按钮
- TOC导航含章节标签 + 返回编辑按钮

**修改6 - 论坛要点解析bug修复 (2026-05-24 第二次会话)**:
- 正则 `/论坛\s*(?:[（(]提问[）)])?\s*(?:要点)?\s*[：:\s]?\s*([\s\S]*?)$/` 兼容"论坛要点"格式
- 抓取后优先查"祷告"/"宣告"边界 (`/\b(?:祷告|宣告)\b/`) 切分论坛/祷告
- 无祷告边界时回退到777关键字分割逻辑
- 新增 `afterForum.replace(/^要点\s*[：:，,]?\s*/, '')` 去掉残留标签后缀

**修改7 - 幻灯片TOC菜单改为V1紧凑圆点风格**:
- CSS: `.slide-toc-item { width:28px; height:22px; border-radius:4px; font-size:10px; }`
- `.slide-toc-item.current { width:auto; min-width:28px; padding:0 8px; font-size:11px; }`
- `.slide-toc-item.section-start { border-left:2px solid var(--amber); }`
- buildToc只显示编号，title属性作为tooltip显示完整名称
- `.slide-toc.hidden` toggle 替代 `.slide-toc.show`
- tocVisible初始值修正为true

**GitHub推送状态**: MCP push_files报403、无gh CLI、无本地token，用户已通过浏览器手动编辑推送
- 2026-06-07 第二次修复：音频播放 "no supported sources" 错误
  - 根因: oss.top237.top 的音频URL含中文字符（如 `54.心向主宝座歌.mp3`），未URL编码导致 Audio 元素无法识别
  - 修复1: `updateAudioForSlide()` 中新增 `encodeURI(audioUrl)` 对远程URL编码非ASCII字符
  - 修复2: `toggleAudio()` 新增 src 有效性检查 + `.play().catch()` 错误处理
  - 修复3: `_attachAudioAndPlay` error listener 增加调试日志
- 2026-06-07 第四次修复：识别并清除过期的音频字典URL
  - oss.top237.top 文件重组，旧 `assets/[32位hash]` 全部404，新格式为中文歌名.mp3
  - `lookupSongUrlFromDict()` 检测hash URL返回null并删除过期条目，回退API
- 2026-06-07 第五次：内置固定歌曲本地音频
  - `audio/24-ni-yuanshi-baogui.mp3` (2.8MB) + `audio/134-zhu-zai-wo-rensheng.mp3` (5.4MB)
  - 更新 `LOCAL_AUDIO_MAP`，`getLocalAudioUrl()` 匹配后直接同源加载，零网络依赖
- 2026-06-07 第六次：TOC菜单显示文字简称 + 音频下载按钮
  - `getSectionAbbr()` 映射章节名→简称（开/赞/诗/编/证/论/祷/结等）
  - `buildToc()` 显示编号+简称双行布局（`toc-num` + `toc-label`）
  - `.slide-toc-item` CSS 改为纵向 mini card 布局，显示简称为主
  - 5 个歌曲卡片各新增 `⬇下载` 按钮（`downloadAudio(type)`）
  - `downloadAudio()` 优先 fetch blob 触发下载，CORS 失败时 fallback 到 `window.open`
